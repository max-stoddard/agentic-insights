{
  "name": "@ai-water-usage/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "node --watch --import tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src test --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-water-usage/shared": "0.1.0",
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.2.1"
  }
}
