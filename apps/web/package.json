{
  "name": "@ai-water-usage/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "lint": "eslint src test vite.config.ts --ext .ts,.tsx",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-water-usage/shared": "0.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
