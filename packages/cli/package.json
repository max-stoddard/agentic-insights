{
  "name": "ai-water-usage",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "ai-water-usage": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/prepare-runtime.mjs",
    "prepack": "npm run build -w @ai-water-usage/shared && npm run build -w @ai-water-usage/server && npm run build -w @ai-water-usage/web && npm run build",
    "lint": "eslint src test --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.2.1"
  }
}
